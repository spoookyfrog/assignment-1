using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class treeScript : MonoBehaviour
{
    public bool gvg = true;

    // Start is called before the first frame update
    void Start()
    {
        print(gvg);

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
