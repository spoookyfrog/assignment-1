using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class treebushScript : MonoBehaviour
{
    public string first = "My name is";

    public string second = "Lakshmi Segura";

    // Start is called before the first frame update
    void Start()
    {


        print(first);
        print(second);
    }

    // Update is called once per frame
    void Update()
    {
     

    }
}
